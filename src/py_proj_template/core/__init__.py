"""Internal core packages for the project template.

The ``lib_example`` subpackage demonstrates how to structure reusable
library code that can be re-exported via the public API layer. Application
workflows live in ``py_proj_template.application``.
"""

